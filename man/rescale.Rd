% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tools.R
\name{rescale}
\alias{rescale}
\title{Linearly Rescale Numeric Values to a New Range}
\usage{
rescale(values, new_min = 10, new_max = 100)
}
\arguments{
\item{values}{A numeric vector of values to be rescaled.}

\item{new_min}{A numeric value specifying the lower bound of the new range.}

\item{new_max}{A numeric value specifying the upper bound of the new range.}
}
\value{
A numeric vector of the same length as \code{values}, with each element rescaled to the new range.
Missing values (\code{NA}) are preserved.
}
\description{
This function rescales a numeric vector from its original range to a new, specified range.
It performs a linear transformation such that the minimum and maximum values of the input
correspond to \code{new_min} and \code{new_max}, respectively. Constant vectors are handled gracefully
by assigning all elements to the midpoint of the new range.
}
\examples{
# Basic usage
rescale(c(2, 4, 6, 8, 10), new_min = 0, new_max = 1)

# Constant vector
rescale(rep(5, 4), new_min = 10, new_max = 20)

# Vector with missing values
rescale(c(1, 2, NA, 3), 0, 10)

}
\keyword{internal}
